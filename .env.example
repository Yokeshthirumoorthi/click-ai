# ClickHouse (localhost on data machine, remote IP on other machines)
CH_HOST=clickhouse
CH_USER=admin
CH_PASSWORD=clickhouse123
CH_DATABASE=otel

# S3 (customer's real bucket in prod, MinIO in dev)
S3_ENDPOINT=http://minio:9000
S3_ACCESS_KEY=minioadmin
S3_SECRET_KEY=minioadmin
S3_BUCKET=traces
S3_TRACES_PREFIX=incoming/
S3_METRICS_PREFIX=metrics/
S3_LOGS_PREFIX=logs/

# S3 backup transport (master CH → session droplets)
S3_BACKUP_BUCKET=ch-backups
S3_REGION=us-east-1

# Agent
OPENROUTER_API_KEY=
